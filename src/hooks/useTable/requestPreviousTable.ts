export const requestPreviousTable = () => {
  // const storage = localStorage.getItem('TABLE_LOG')
  // if (storage) {
  // const body = JSON.parse(storage)
  // if (body.filters.length === 0) {
  localStorage.setItem('REQ_PREV_TABLE', 'true')
  // }
  // }
}
